# Материалы: часть 7 из 8

Пока только прими материалы. Приступай к созданию после сообщения «Все материалы переданы, начинай».

## Слайд 17

Установка TCP-соединения в Wireshark

Разберем подробно, что мы видим из второго этапа рукопожатия:
1. Сам пакет, который мы рассмотрим изнутри.
2. Сервер отправляет ответ на клиент, используя в качестве порта источника порт
443, а в роли порта назначения – частный порт 54824. Это происходит потому, что
в ответе роли портов меняются местами.
3. В данном пункте необходимо внимательно разобраться. Мы видим строчки:
Sequence Number: 0 – Важно понимать, что здесь тоже ноль, потому что
это первая последовательность со стороны сервера.
Sequence Number (raw): 1565342372 – Здесь представлен настоящий,
«сырой» номер, присвоенный пакету с которым он передается по сети.
Next Sequence Number: 1 – это следующий ожидаемый номер
последовательности, который должен прийти. То есть, +1 к текущему.
Acknowledgment number и Acknowledgment number (raw) – здесь
представлены номера подтверждающей последовательности (флаг ACK). Указано
1 и 3638817701, потому что +1 к начальному номеру последовательности.
4. Установленный флаг ACK обозначает подтверждение принятых данных.
5. Установленный флаг SYN обозначает начало синхронизации (со стороны
сервера в данном случае) и запрос на соединение с клиентом.
6. Window 65160 – Как мы видим, размер окна изменился. Это связано с
механизмом динамического изменения размера окна (так называемое «скользящее
окно»), который будет подробно рассмотрен далее.

![Иллюстрация исходного слайда 17](images/slide-17-image-12.png)

---

## Слайд 18

Установка TCP-соединения в Wireshark

Разберем подробно, что мы видим из третьего этапа рукопожатия:
1. Сам пакет, который мы рассмотрим изнутри.
2. Вновь порта поменялись местами, потому что это данные от клиента для
сервера.
3. В данном пункте необходимо внимательно разобраться. Мы видим строчки:
Sequence Number: 1 – Последовательность сменилась на единицу.
Sequence Number (raw): 3638817701 – Видим закономерное +1.
Next Sequence Number: 1 – это следующий ожидаемый номер
последовательности, который должен прийти. То есть, +1 к текущему, что логично.
Acknowledgment number и Acknowledgment number (raw) – здесь
представлены номера подтверждающей последовательности (флаг ACK). Указано
1 и 1565342373, потому что +1 к начальному номеру последовательности.
4. Установленный флаг ACK обозначает подтверждение принятых данных.
5. Window 502 – Размер окна снова изменился. Он будем изменяться каждый раз
в зависимости от параметров принимающей и передающей стороны, а так же от
загруженности сети.

![Иллюстрация исходного слайда 18](images/slide-18-image-13.png)

---

## Слайд 19

Надежность TCP

Подтверждение и размер окна

При трехстороннем квитировании в TCP-соединении обе стороны уведомляют друг
друга о максимальном количестве байтов (размере буфера), которые могут быть
получены локальной стороной с помощью поля Окно.
2. После установления TCP-соединения отправитель отправляет данные указанного
количества байтов на основе размера окна, заявленного получателем.
3. После получения данных получатель хранит данные в буфере и ожидает
получения буферизированных данных приложением верхнего уровня. После
получения данных приложением верхнего уровня освобождается соответствующее
пространство в буфере.
4. Получатель сообщает текущий приемлемый размер данных (окна) в соответствии с
его размером буфера.
5. Отправитель отправляет определенный объем данных в зависимости от текущего
размера окна получателя.

![Иллюстрация исходного слайда 19](images/slide-19-image-14.png)

---

## Слайд 20

Надежность TCP

Подтверждение и размер окна

приведен пример передачи данных при TCP-соединении с
размером окна, равным 3000 байт, который был согласован в процессе установления
TCP-соединения. После передачи получателю 3000 байт, последний уведомляет
отправителя об успешном приеме сообщением с флагом ACK, и отправитель начинает
передавать следующий блок данных.

![Иллюстрация исходного слайда 20](images/slide-20-image-15.png)

---

## Слайд 21

Надежность TCP

Потеря данных и повторная передача

В любой, даже хорошо организованной сети, обычно возникают потери данных.
Чтобы справиться с этой проблемой, протокол TCP имеет механизм повторной передачи
сегментов. При получении сегментов целевым узлом подтверждаются только те данные,
которые поступили в непрерывной последовательности байт. Если после истечения
таймаута узел-отправитель не получил подтверждение о доставке, он повторно
отправляет сегменты, начиная с последнего полученного подтверждения (ACK).
К примеру, были получены сегменты с порядковыми номерами от 1 до 3000 и от
6001 до 9000, то номер ACK будет равен 3001.

![Иллюстрация исходного слайда 21](images/slide-21-image-16.png)

---

## Слайд 22

Надежность TCP

Завершение TCP-сеанса

1) После завершения отправки данных, узел А отправляет сегмент с установленным
в заголовке флагом FIN для прекращения сессии «клиент-сервер»;
2) Узел Б принимает запрос на завершение соединения и отправляет подтверждение
ACK узлу А;
3) Узел Б отправляет сегмент с установленным в заголовке флагом FIN для
прекращения сессии «сервер-клиент». ;
4) Узел А, в свою очередь, отправляет ответ ACK для подтверждения получения FIN
от узла Б.

![Иллюстрация исходного слайда 22](images/slide-22-image-17.png)

---

## Слайд 23

Надежность TCP

Завершение TCP-сеанса

ПК1 отправляет TCP-сегмент с установленным флагом FIN. Сегмент не
содержит данных.
2. После получения ПК1 сегмента TCP ПК2 отвечает сегментом TCP с
установленным флагом ACK.
3. ПК2 проверяет необходимость отправки данных. Если необходимо, ПК2
отправляет данные, а затем TCP-сегмент с флагом FIN для закрытия
соединения. Если нет, ПК2 напрямую отправляет TCP-сегмент с
установленным флагом FIN.
4. После получения TCP-сегмента с установленным флагом FIN ПК1 отвечает
сегментом с флагом ACK. Затем TCP-соединение разрывается в обоих
направлениях.

![Иллюстрация исходного слайда 23](images/slide-23-image-18.png)

---

## Слайд 24

Установление TCP-соединения и управление потоком

Данные при передаче по протоколу TCP делятся на сегменты, которые при выборе
различных путей маршрутизации могут быть доставлены на узел назначения в
измененном порядке. Для того чтобы воссоздать исходный порядок сегментов, протокол
TCP использует порядковые номера в заголовках каждого сегмента.
При установлении сеанса связи задается начальный порядковый номер сеанса
(ISN), представляющий собой случайное начальное значения счетчика байт, которые
были переданы целевому узлу. При дальнейшей передаче сегментов указанное число
увеличивается на определенное число байт, что позволяет контролировать передачу
данных.
Стоит обратить внимание, что ISN не является строго определенным числом, а
задается случайно. Таким образом, можно предотвратить осуществление вредоносных
атак.
На узле назначения все полученные сегменты распределяются в правильном
порядке и только после этого передаются на уровень приложений.

---

## Слайд 25

Протокол UDP

Функции UDP

Протокол UDP так же, как и TCP, использует сегментацию данных, при этом не
задействует процессы, отвечающие за уведомление об успешном получении сегмента,
что позволяет значительно быстрее передавать данные. Пользуясь преимуществом в
скорости и отсутствием подтверждения полученной информации, протокол UDP
используется при передаче данных которые могут перенести кратковременные потери во
время передачи, например, потоковой видео и звуковой информации, где потеря одной
или нескольких датаграмм, т.е. с незначительными перебоями, может не повлиять на
общую ситуацию в целом во время передачи.

---

## Слайд 26

Основные характеристики протокола UDP:

Протокол UDP

Основными характеристиками протокола UDP являются:
• Отсутствие установления сеанса связи;
• Отсутствие повторной отправки потерянных сегментов;
• Наличие высокоскоростного потока.
Части сообщения в UDP, передающиеся без предварительного установления
канала и последующей гарантии передачи, называются датаграммами. У датаграммы
есть заголовок, состоящий из 8 байт и включающий в себя порт источника (16 бит), порт
назначения (16 бит), длину (16 бит) и контрольную сумму (16 бит).

---

## Слайд 27

Протокол UDP

UDP-датаграммы

Датаграмма – блок информации, передающийся с помощью протокола UDP, без
гарантированной доставки на узел назначения.

![Иллюстрация исходного слайда 27](images/slide-27-image-19.png)

---

## Слайд 28

Сравнение функций протоколов TCP и UDP

![Иллюстрация исходного слайда 28](images/slide-28-image-20.png)

---

## Слайд 29

Основные типы приложений, которые используют UDP

Приложения, которые могут самостоятельно гарантировать полную передачу
данных например, TFTP. В таком случае, контроль правильности передачи данных
осуществляется приложением 7-го уровня модели OSI, а не самим протоколом;
• Приложения, осуществляющие только отправку запросов и получение ответов
(например, DHCP);
• Приложения для передачи мультимедийного контента в режиме реального
времени (например, VoIP)


---

# Таблицы и текстовые описания иллюстраций

Источник: изображения из 29-слайдовой PPTX. Таблицы ниже переписаны с изображений, описания схем кратко передают видимую структуру и поясняющий текст слайдов. Это не посимвольное OCR всех мелких подписей. Для воспроизведения учебного процесса используйте вместе с `03-presentation.md`.

## Слайд 5. Сегментация

Файл «300 КБАЙТ» разделяется на пронумерованные сегменты. Под каждым сегментом показан добавляемый заголовок. Направление чтения: файл → части → части с заголовками. На картинке подписано «1500 байт», а в тексте PDF приводится пример 1460 байт; не считать эти подписи взаимозаменяемыми без пояснения.

## Слайд 6. Мультиплексирование

Несколько потоков приложений обозначены разными цветами. Их блоки чередуются в общем канале через сетевое устройство и направляются к разным приложениям получателя. Для новой схемы нужны: несколько источников, общий канал с чередующимися блоками, разветвление к получателям и цветовая легенда.

## Слайд 7. Известные порты — транскрипция таблицы

| Сервис | Порт в исходной таблице |
| --- | --- |
| HTTP | 80 |
| SSH | 22 |
| DNS | 53 |
| POP3 | 110 |
| IMAP | 143 |
| FTP (команды) | 21 |
| FTP (передача) | 20 |
| TFTP | 69 |
| Telnet | 23 |
| HTTPS | 443 |
| NTP | 123 |
| DHCP (клиент) | 67 |
| DHCP (сервер) | 68 |
| SMTP | 25 |

Это точная передача подписей исходной таблицы, а не независимая проверка назначений портов. Роли DHCP и соответствующие номера требуют проверки перед использованием как справочника.

## Слайд 8. Сокеты и адресация

Два горизонтальных примера обмена между клиентом и сервером: HTTP и FTP. У каждого узла показан IP-адрес, у приложения — номер порта. Стрелки запросов и ответов показывают, что адреса и порты источника и назначения меняются местами. В HTTP-примере виден серверный порт 80, в FTP-примере — 20. Изображение сопровождается пояснением, что связка адреса и порта указывает на приложение на конкретном устройстве.

