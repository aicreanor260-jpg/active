# Материалы: часть 3 из 8

Пока только прими материалы. Приступай к созданию после сообщения «Все материалы переданы, начинай».

## Страница 7

• Установление сеанса связи. Перед пересылкой трафика протокол устанавливает
соединение между узлом передачи и узлом приема. Это позволяет контролировать
передачу данных и согласовывать объем трафика;
• Регулирование потока передачи данных. При активном использовании
вычислительных мощностей сетевых узлов протокол TCP может скорректировать
количество передаваемой информации. Данная функция позволяет предотвратить
перегрузку узлов, а также повторную передачу данных.
Каждый отправляемый сегмент данных содержит дополнительно от 20 байт (160
бит) до 60 байт (480 бит) в заголовке; рассмотрим их подробнее.
8.4.2. Заголовок TCP-сегмента
Рисунок 8.4.2.1. – Заголовок TCP-сегмента
Заголовок TCP-сегмента содержит от 20 до 60 байт:
1. Порт источника и порт назначения (по 16 бит) определяют порты отправителя
и получателя.
2. Порядковый номер (32 бита) в сегменте используется для контроля порядка
сегментов.
3. Номер подтверждения (32 бита) необходим для подтверждения успешного
получения узлом определенного сегмента.
4. Длина заголовка (смещение данных) (4 бита) – длина заголовка сегмента TCP в
«словах», то есть в группах по 4 байта (32 бита).
5. Зарезервировано (6 бит) – это биты, которые могут быть использованы в

## Страница 8

будущем.
6. Управляющие биты (6 бит) содержат флаги, определяющие назначение и
функции сегмента TCP.
7. Размер окна (16 бит) указывает количество байт данных, которые получатель
может принять в данный момент.
8. Контрольная сумма (16 бит) используется для проверки наличия ошибок в
сегменте.
9. Срочность (16 бит) определяет данные, которые должны быть переданы сразу.
Поле управляющих битов
Для управления сеансом связи в заголовке протокола TCP используется поле
длинной 6 бит. Данные биты называются флагами.
Рисунок 8.4.2.2. – Представление управляющих флагов в Wireshark. Флаг Syn
Поле управляющих битов может иметь следующие значения:
• SYN – флаг синхронизации порядковых номеров. Когда флаг SYN установлен, в
поле «порядковый номер» передается изначальный номер последовательности
(Initial Sequence Number, ISN). Первый байт данных будет передан в следующем
пакете с номером последовательности, равным ISN+1.
• ACK – флаг подтверждения. Данный флаг указывает на то, что в поле «номер
подтверждения» указан следующий порядковый номер сегмента, который
ожидается для получения на приемной стороне. Также, данный флаг подтверждает
приём предыдущих сегментов.
• FIN – флаг завершения сеанса связи. Если такой флаг установлен, это означает,
что отправитель сигнализирует о завершении передачи данных и не планирует
отправлять какие-либо дополнительные данные в рамках текущего соединения.
• RST – флаг сброса соединения. Установка этого флага означает немедленный
разрыв соединения и игнорирование всех последующих входящих данных из этого
соединения.
• PSH – флаг функции push. Когда такой флаг установлен, это означает, что
необходимо передать все данные из буфера памяти в обработку, даже если он не
был заполнен. Здесь важно сделать отступление и сказать, что протокол TCP
использует буферизированную передачу данных как на отправке, так и на приеме.
То есть, данные перед отправкой поступают в буфер, и при его заполнении
отправляются по адресу. Такой метод позволяет снизить количество служебного
трафика (заголовков) в сети.

## Страница 9

• URG – флаг указателя важности. Установка данного флага означает, что данные
в пакете необходимо обработать в приоритетном порядке.
8.4.3. Установка TCP-соединения
Рисунок 8.4.3.1. – Процесс установки TCP-соединения
Прежде, чем начать передачу данных, пользователь устанавливает соединение с
сервером, такой процесс называется трехэтапным рукопожатием. Данный процесс
согласует номера используемых портов и порядковые номера сегментов. Процесс
установление TCP соединения проходит в три этапа:
1) Узел А, инициализирующий сессию для обмена данными «клиент-сервер»,
отправляет на узел Б запрос SYN;
2) Узел Б подтверждает запрос на инициализацию сессии для обмена данными
«клиент-сервер», отправляет узлу А ответ ACK и запрос SYN «сервер-клиент»;
3) В конце узел А подтверждает запрос SYN «сервер-клиент» и отправляет ACK узлу
Б.
Для того чтобы подробнее изучить, как происходит процесс трехэтапного
рукопожатия, рассмотрим реальный пример на основе программы анализатора сетевого
трафика Wireshark.
Для начала переходим в командную строку Windows, где c помощью команды
nslookup узнаем IP-адрес веб-сайта www.eltex-co.ru, в браузере переходим на веб-
страницу www.eltex-co.ru, но перед этим запускаем программу Wireshark и начинаем
захватывать трафик на интерфейсе.
В фильтре Wireshark вводим команду ip.addr == 62.109.1.166 (IP-адрес веб-сайта
www.eltex-co.ru), которая покажет все захваченные пакеты, где есть адрес 62.109.1.166.

## Страница 10

Рисунок 8.4.3.2. – Первый этап «трехэтапного рукопожатия»
Разберем подробно, что мы видим из первого этапа рукопожатия:
1. Сам пакет, который мы рассмотрим изнутри.
2. Клиент отправляет запрос на сервер, используя в качестве порта источника
частный порт 54824, а в роли порта назначения – общеизвестный порт 443 (HTTPS).
3. В данном пункте необходимо внимательно разобраться. Мы видим строчки:
Sequence Number: 0 – Ноль в данной строке обозначает порядковый номер
относительно начала сессии. Поскольку эта последовательность является самой
первой, она обозначается нулем. Важно понимать, что это значение сформировано
программой, для упрощения нашего понимания начала и конца
последовательности.
Sequence Number (raw): 3638817700 – Здесь представлен настоящий,
«сырой» номер, присвоенный пакету, с которым он передается по сети.
Next Sequence Number: 1 – это следующий ожидаемый номер
последовательности, который должен прийти. То есть, +1 к текущему, что логично.
Acknowledgment number и Acknowledgment number (raw) – здесь
представлены номера подтверждающей последовательности (флаг ACK). Ноль
потому, что ACK еще не поступил.
4. Установленный флаг SYN обозначает начало синхронизации и запрос на
соединение с сервером.
5. Window 64240 – в данном случае это размер окна приема TCP. Оно обозначает
максимальное количество байтов данных, которое может быть принято
принимающей стороной в данный момент.

## Страница 11

Рисунок 8.4.3.3. – Второй этап «трехэтапного рукопожатия»
Разберем подробно, что мы видим из первого этапа рукопожатия:
1. Сам пакет, который мы рассмотрим изнутри.
2. Сервер отправляет ответ на клиент, используя в качестве порта источника порт
443, а в роли порта назначения – частный порт 54824. Это происходит потому, что
в ответе роли портов меняются местами.
3. В данном пункте необходимо внимательно разобраться. Мы видим строчки:
Sequence Number: 0 – Важно понимать, что здесь тоже ноль, потому что
это первая последовательность со стороны сервера.
Sequence Number (raw): 1565342372 – Здесь представлен настоящий,
«сырой» номер, присвоенный пакету с которым он передается по сети.
Next Sequence Number: 1 – это следующий ожидаемый номер
последовательности, который должен прийти. То есть, +1 к текущему.
Acknowledgment number и Acknowledgment number (raw) – здесь
представлены номера подтверждающей последовательности (флаг ACK). Указано
1 и 3638817701, потому что +1 к начальному номеру последовательности.
4. Установленный флаг ACK обозначает подтверждение принятых данных.
5. Установленный флаг SYN обозначает начало синхронизации (со стороны
сервера в данном случае) и запрос на соединение с клиентом.
6. Window 65160 – Как мы видим, размер окна изменился. Это связано с
механизмом динамического изменения размера окна (так называемое «скользящее
окно»), который будет подробно рассмотрен далее.

## Страница 12

Рисунок 8.4.3.4. – Третий этап «трехэтапного рукопожатия»
Разберем подробно, что мы видим из третьего этапа рукопожатия:
1. Сам пакет, который мы рассмотрим изнутри.
2. Вновь порта поменялись местами, потому что это данные от клиента для
сервера.
3. В данном пункте необходимо внимательно разобраться. Мы видим строчки:
Sequence Number: 1 – Последовательность сменилась на единицу.
Sequence Number (raw): 3638817701 – Видим закономерное +1.
Next Sequence Number: 1 – это следующий ожидаемый номер
последовательности, который должен прийти. То есть, +1 к текущему, что логично.
Acknowledgment number и Acknowledgment number (raw) – здесь
представлены номера подтверждающей последовательности (флаг ACK). Указано
1 и 1565342373, потому что +1 к начальному номеру последовательности.
4. Установленный флаг ACK обозначает подтверждение принятых данных.
5. Window 502 – Размер окна снова изменился. Он будем изменяться каждый раз
в зависимости от параметров принимающей и передающей стороны, а так же от
загруженности сети.

## Страница 13

8.4.4. Надежность TCP
8.4.4.1. Подтверждение и размер окна
Рисунок 8.4.4.1.1. – Процесс передачи данных при TCP-соединении
В заголовке TCP-сегмента существует 16-битное поле для указания размера окна.
Данный параметр необходим для управления объемом данных, который целевой узел
может успешно принять и обработать.
Первичный размер окна согласуется при установлении сеанса в рамках
трехэтапного рукопожатия. При получении информации о размере окна получателя,
отправитель должен ограничить количество одновременно передаваемых байт.
Как правило, получатель не дожидается получения всех пакетов, прежде чем
отправить подтверждение, а делает это после получения каждых 2 сегментов (количество
может варьироваться). Такой процесс называется скользящее окно. За счет непрерывной
передачи сегментов повышается скорость работы сети, но в случае отсутствия
подтверждения с принимающей стороны необходимо будет повторно переслать эти
данные и уменьшить размер окна, чтобы снизить объём передаваемого трафика. Это
может помочь в пиковые моменты, когда принимающая сторона перегружена.
На рисунке 8.4.6.1.1 приведен пример передачи данных при TCP-соединении с
размером окна, равным 3000 байт, который был согласован в процессе установления
TCP-соединения. После передачи получателю 3000 байт, последний уведомляет
отправителя об успешном приеме сообщением с флагом ACK, и отправитель начинает
передавать следующий блок данных.
8.4.4.2. Потеря данных и повторная передача
В любой, даже хорошо организованной сети, обычно возникают потери данных.
Чтобы справиться с этой проблемой, протокол TCP имеет механизм повторной передачи
сегментов. При получении сегментов целевым узлом подтверждаются только те данные,
которые поступили в непрерывной последовательности байт. Если после истечения
таймаута узел-отправитель не получил подтверждение о доставке, он повторно
отправляет сегменты, начиная с последнего полученного подтверждения (ACK).
К примеру, были получены сегменты с порядковыми номерами от 1 до 3000 и от
6001 до 9000, то номер ACK будет равен 3001.

